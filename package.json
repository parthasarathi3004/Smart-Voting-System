{
  "name": "biometric-evoting-platform",
  "version": "1.0.0",
  "description": "Production-Grade Biometric-Secured Anti-Fraud E-Voting & Analytics Platform",
  "scripts": {
    "build": "npm install --prefix server && npm install --prefix client && npm run --prefix client build",
    "start": "node server/server.js",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "npm --prefix server start",
    "client": "npm --prefix client run dev",
    "install:all": "npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
