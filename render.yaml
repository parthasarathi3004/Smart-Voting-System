services:
  - type: web
    name: smart-voting-system
    env: node
    plan: free
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: ADMIN_USERNAME
        value: parthasarathi
      - key: ADMIN_PASSWORD
        value: gvtvote@123
